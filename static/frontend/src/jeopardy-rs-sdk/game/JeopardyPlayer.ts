// this is not generated from the rust backend
// because the frontend representation is a read-only 
// view of the player state
export interface JeopardyPlayer {
    points: number,
    name: string,
    buzzed: boolean,
}